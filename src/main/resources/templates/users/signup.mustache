{{>layout/header}}
{{>layout/nav}}
<div class="container">
    <form>
        <div class="form-group">
            <label for="id">Имя</label>
            <input type="text" class="form-control" id="id" placeholder="Пожалуйста, введите ваш Имя" required>
        </div>
        <div class="form-group">
            <label for="pw">Пароль</label>
            <input type="password" class="form-control" id="pw" placeholder="пароль" required>
        </div>
        <div class="form-group">
            <label for="name">Логин</label>
            <input type="text" class="form-control" id="name" placeholder="Введите свой логин, пожалуйста" required>
        </div>
    </form>
    <button type="button" id="btn-signup" class="btn btn-secondary">Зарегистрироваться</button>
</div>
{{>layout/footer}}